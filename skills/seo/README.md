# seo

This is a short-command alias for `senior-open-source-engineer`.

Place `seo/` next to `senior-open-source-engineer/` inside your `skills/` directory when your agent maps slash commands from folder names and you want `/seo` to invoke the same framework.

The alias delegates to the full framework in the sibling `senior-open-source-engineer/` directory.
